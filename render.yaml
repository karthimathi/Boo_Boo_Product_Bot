services:
  - type: worker
    name: telegram-products-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: PRODUCTS_URL
        sync: false
    autoDeploy: true
